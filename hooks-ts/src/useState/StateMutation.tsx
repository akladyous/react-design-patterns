import { useState } from 'react';
import { usersData } from '../data/usersData';
type Props = {};

export default function StateMutation(props: Props) {
  const [users, setUsers] = useState({});

  return (
    <section>
      <div></div>
    </section>
  );
}
