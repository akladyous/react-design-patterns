import UserFormContext from './UserFormContext';

type Props = {};

export default function UseContextDemo(props: Props) {
  return (
    <section>
      <>
        <UserFormContext />
      </>
    </section>
  );
}
